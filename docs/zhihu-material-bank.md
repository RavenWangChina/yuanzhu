# 知乎内容素材库（已脱敏，2026-09-15 建）

> 供后续回答/文章引用。三级分类：A=公开可直接用；B=脱敏后可用（公司名/平台名/人名已去除）；C=禁用红线。
> 每次写作从这里取材，不在写作时现想。

## A 级：元铸工坊（公开仓库 RavenWangChina/yuanzhu，直接引用）

**版本与测试爬坡**（硬数据）：
- v0.1.2→0.1.11，测试数 142→145→150→155→160→161（每个小版本 +3~5）
- 8 个里程碑全部有 CHANGELOG 记录，0.1.4/0.1.5 都是 dogfood 发现的问题修复版

**dogfood 一手故事**（0.1.4/0.1.5 CHANGELOG）：
- "真实使用实测抓到四项"：假进度条换真实步骤进度（"独立审查… 3/6 步"）、排队提问、流内洞见确认、模式检测可解释
- forge 硬化：AI 生成工作流时猜字段名 → 三步 schema 补全（query_step/ai_step/action_step）+ dry-run 门禁"结构问题在生成时就拒绝，不留坏草稿让人踩坑"
- 400 错误带修复提示："检测到 query.type=… 应写成 object_type"——不再 500

**三记忆推衍 E2E 实测**（0.1.6）：问 2 个问题 → 推衍引擎 7 秒产出 3 条提议 → 采纳一条自动触发 forge 上架
**自举**：行为埋点五类（ask/adopt/approve/reject/forge）→ Episodic Memory → 推衍引擎交叉三记忆 → Proposition
**版本号回调事件**：0.2.0→0.1.2（版本递进由用户裁决，AI 不得自主跳级——治理案例）
**开发节奏**：v0.1 三根柱（H1 双输入工坊/H2 非技术者可用/H7 轻量本体）验证式开发

## B 级：工程坑与 AI 协作实践（脱敏后用）

**AI 编程实战坑**（来自元铸工坊 v0.1 开发，公开仓库关联，可轻度使用）：
- 安全工具拦 Edit 操作 → 用 Python 脚本绕行（工具对抗的日常）
- curl 传中文 JSON 必乱码 → 换 urllib（跨工具数据传递的编码雷区）
- FastAPI get_db 依赖必须是裸 async generator——AI 生成的代码常踩框架惯用法

**基础设施排查**（脱敏：不提任何厂商名）：
- "context window limit"报错排查三天，真因是 API 网关硬限 max_tokens 上限，客户端兜底文案误导方向——教训：400 类错误先用最小请求复现测网关，别先改本地配置
- Windows Python 默认 GBK 输出，UTF-8 环境读中文必乱码——环境事实先于代码 bug

**多会话 AI 协作模式**（脱敏：不提具体项目）：
- 长任务跨会话：交接文档传统（handoff 文档：状态/待办/凭据位置/红线），新会话 5 分钟接续
- 跨会话记忆不可靠时的对策：重要决策写进项目文档而非依赖对话记忆
- 每轮交付强制提炼"情景→规则"（这次 X 坑，以后 Y），同类失败第二次必须成模式

**今日实测数据点**（可公开，本身就是透明度叙事）：
- 2.5 小时产出 5 回答+1 文章+6 评论，每篇走六步思考流水线（deep-answer）
- 一个完整回答的生产链：选题分析→双视角对抗→审计→裁决→写作→发布，全程约 20 分钟
- fengbin 质疑"AI 味道"的回复本身成为透明度叙事素材

**个人项目**（脱敏：不提数据源厂商/key）：
- ~~盯盘平台 stock-monitor~~ **（2026-09-15 用户裁决：整体禁用）**——本地含个人配置（隐私层），金融类个人项目（旺财生态三仓：盯盘/策略库/数据底座）**一概不提**：不点名、不提功能细节、不建立知乎身份与该项目的关联

**⚠️ 旺财生态（2026-09-15 用户两次裁决收紧）——整块禁用**：
- 795 个策略及绩效统计：仅限内部参考，禁止公开引用（第三方采集非原创）
- stock-monitor 等金融类个人项目：**不公开**（隐私层配置），代码开源与否都不在知乎内容中提及
- 金融话题可以答（如 C2 方法论），但只用通用工程知识，不带任何旺财元素

## C 级：禁用红线（永不引用）

- 任何公司名/客户名/产品代号（企业项目一律说"我参与的企业项目"且不展开细节）
- 真实同事姓名
- 内部数据的具体数字（可抽象为比例/模式，不给绝对值）
- API 网关/云厂商名称（说"某网关"）
- 生产服务器信息

## 使用规则

1. B 级素材引用前自查：去掉所有专有名词后故事是否仍然成立
2. 同一素材在相邻两篇不重复使用（同质化红线）
3. 数据类素材标注时点（"截至 v0.1.11"）
